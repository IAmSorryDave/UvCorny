repos:
  - repo: local
    hooks:
      - id: sync-metadata
        name: Sync project metadata
        entry: uv run python scripts/sync_metadata.py
        language: system
        files: (pyproject\.toml|README\.md\.jinja)$
        stages: [commit]
        pass_filenames: false

