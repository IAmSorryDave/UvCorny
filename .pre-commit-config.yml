repos:
  - repo: local
    hooks:
      - id: sync-metadata
        name: Sync project metadata
        entry: python .python/sync_metadata.py
        language: python
        additional_dependencies: ["jinja2"]
        files: (pyproject\.toml|README\.md\.jinja)$
        stages: [commit]
        pass_filenames: false

