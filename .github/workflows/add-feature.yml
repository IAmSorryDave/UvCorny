name: Add ➕ Feature 🔧

on:
  push: 
    branches:
      - features

jobs:

  increment:

    if: always()
    runs-on: ubuntu-latest
    permissions: write-all
    
    steps:
    
    - name: Checkout Project
      uses: actions/checkout@v6
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Install UV
      uses: astral-sh/setup-uv@v7
    
    - name: Dry Run Version Increment
      id: is_alpha
      run: |
        uv version --bump alpha --dry-run

    - name: Version Increment Alpha Alone
      if: steps.is_alpha.outcome == 'success'
      run: |
        uv version --bump alpha

    - name: Version Increment Minor as Alpha 
      if: steps.is_alpha.outcome == 'failure'
      run: |
        uv version --bump minor --bump alpha
    
    - name: Verify Version Changed
      run: git diff --exit-code pyproject.toml || echo "Version updated successfully"

    - name: Tag release
      uses: stefanzweifel/git-auto-commit-action@v7
      with:
        branch: tests
        commit_message: "Released $(uv version)"
        tag_name: "v$(uv version)"
  
