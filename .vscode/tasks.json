{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Git commit (skip pytest on alpha branch)",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "BRANCH=$(git rev-parse --abbrev-ref HEAD); if [[ \"$BRANCH\" == \"alpha\" ]]; then SKIP=pytest git commit -m '${input:commitMessage}'; else git commit -m '${input:commitMessage}'; fi"
      ],
      "inputs": [
        {
          "id": "commitMessage",
          "description": "Commit message",
          "type": "promptString"
        }
      ]
    }
  ]
}
